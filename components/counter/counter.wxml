<l-counter
  l-count-class="all-background input-size"
  l-symbol-class="all-background"
  l-disable-class="all-background"
  l-class="container"
  count="{{count}}"
  min="{{min}}"
  max="{{max}}"
  bind:linout="onOverStep"
/>
